server:
  port: 8087

spring:
  cloud:
    stream:
      bindings:
        input-event:
          destination: event
          group: cloud
        output-event:
          destination: event
          group: cloud